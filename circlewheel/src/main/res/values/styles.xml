<resources>

    //默认的弹窗style
    <style name="DefaultDialogStyle">
        <item name="android:windowBackground">@android:color/transparent</item>     //提示框的背景颜色是什么
        <item name="android:windowFrame">@null</item>                               //是否有边框
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>                           //提示框是否是浮动的
        <item name="android:windowIsTranslucent">true</item>                        //提示框是否是透明的
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>  // 动画
        <item name="android:backgroundDimEnabled">true</item>                           //是否充许对话框的背景变暗。为true则充许变暗
    </style>

</resources>
